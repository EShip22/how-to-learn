@keyframes rotation {
	from {
		transform: rotate(0);
	}
	to {
		transform: rotate(360deg);
	}
}
.rotation {
	/*transform: translateX(-20px);*/
	animation: rotation 40s linear infinite;
	overflow: hidden;
}